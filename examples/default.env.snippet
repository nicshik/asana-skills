# Copy into a local, git-ignored .env.local (never commit a real token).
# Create the token at https://app.asana.com/0/my-apps -> Personal access tokens.
ASANA_ACCESS_TOKEN=<asana-personal-access-token>
